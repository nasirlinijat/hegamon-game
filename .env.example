# Copy to .env.local for local dev.
# The client uses this to locate the WebSocket server.
# In production, set VITE_SERVER_URL to your deployed server URL.
# Leave empty to connect to the same origin (works when Vite proxies /socket.io to localhost:3001).
VITE_SERVER_URL=

# The server listens on this port.
PORT=3001
